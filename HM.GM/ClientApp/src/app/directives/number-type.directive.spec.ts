import { NumberTypeDirective } from './number-type.directive';

describe('NumberTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberTypeDirective();
    expect(directive).toBeTruthy();
  });
});
